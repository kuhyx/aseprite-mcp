# #0001 Open by nature: pixel_read.py's four `if not success:` process-failure guards are structurally unreachable — Aseprite exits 0 even on a corrupt file, so the script's own "ERROR:No active sprite" line is what catches it. Covered only by mocking execute_lua_script directly. Kept deliberately rather than deleting real error handling. Do not "fix" by removing them.

- 2026-08-15T13:59:12Z `issue`: Open by nature: pixel_read.py's four `if not success:` process-failure guards are structurally unreachable — Aseprite exits 0 even on a corrupt file, so the script's own "ERROR:No active sprite" line is what catches it. Covered only by mocking execute_lua_script directly. Kept deliberately rather than deleting real error handling. Do not "fix" by removing them. [aseprite_mcp/tools/pixel_read.py]
